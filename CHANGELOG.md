# Changelog

## [Unreleased]

### Fix

- Updated `nuxt.config.ts` i18n module configuration to search for locale files under the new `app/locales/features/game/` structure by specifying `restructureDir: 'app'` and `langDir: 'locales/'`.
